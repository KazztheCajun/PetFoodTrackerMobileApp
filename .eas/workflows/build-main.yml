name: Build Main Push

on:
  push:
    branches:
      - main

jobs:
  build_android:
   name: Build For Android
   type: build
   params:
     platform: android
  build_ios:
   name: Build for iOS
   type: build
   params:
     platform: ios